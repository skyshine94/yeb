<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myself.server.mapper.MenuMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.myself.server.pojo.Menu">
        <id column="id" property="id" />
        <result column="url" property="url" />
        <result column="path" property="path" />
        <result column="component" property="component" />
        <result column="name" property="name" />
        <result column="iconCls" property="iconCls" />
        <result column="keepAlive" property="keepAlive" />
        <result column="requireAuth" property="requireAuth" />
        <result column="parentId" property="parentId" />
        <result column="enabled" property="enabled" />
    </resultMap>

    <resultMap id="Menus" type="com.myself.server.pojo.Menu" extends="BaseResultMap">
        <collection property="children" ofType="com.myself.server.pojo.Menu">
            <id column="id2" property="id"/>
            <result column="url2" property="url"/>
            <result column="path2" property="path"/>
            <result column="component2" property="component"/>
            <result column="name2" property="name"/>
            <result column="iconCls2" property="iconCls"/>
            <result column="keepAlive2" property="keepAlive"/>
            <result column="requireAuth2" property="requireAuth"/>
            <result column="parentId2" property="parentId"/>
            <result column="enabled2" property="enabled"/>
        </collection>
    </resultMap>

    <resultMap id="MenusWithRole" type="com.myself.server.pojo.Menu" extends="BaseResultMap">
        <collection property="roles" ofType="com.myself.server.pojo.Role">
            <id column="rid" property="id"/>
            <result column="rname" property="name"/>
            <result column="rnameZh" property="nameZh"/>
        </collection>
    </resultMap>

    <resultMap id="AllMenus" type="com.myself.server.pojo.Menu">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <collection property="children" ofType="com.myself.server.pojo.Menu">
            <id column="id2" property="id"/>
            <result column="name2" property="name"/>
            <collection property="children" ofType="com.myself.server.pojo.Menu">
                <id column="id3" property="id"/>
                <result column="name3" property="name"/>
            </collection>
        </collection>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, url, path, component, name, iconCls, keepAlive, requireAuth, parentId, enabled
    </sql>

    <!--根据用户id获取菜单列表-->
    <select id="getMenusByAdminId" resultMap="Menus">
        select distinct
            m1.id,
            m1.url,
            m1.path,
            m1.component,
            m1.name,
            m1.iconCls,
            m1.keepAlive,
            m1.requireAuth,
            m1.parentId,
            m1.enabled,
            m2.id as id2,
            m2.url as url2,
            m2.path as path2,
            m2.component as component2,
            m2.name as name2,
            m2.iconCls as iconCls2,
            m2.keepAlive as keepAlive2,
            m2.requireAuth as requireAuth2,
            m2.parentId as parentId2,
            m2.enabled as enabled2
        from
            t_menu m1
        join
            t_menu m2
        on
            m1.id = m2.parentId
        join
            t_menu_role mr
        on
            m2.id = mr.mid
        join
            t_admin_role ar
        on
            mr.rid = ar.rid
        where
            ar.adminId = #{id}
          and
            m2.enabled = TRUE
        order by
            m1.id,
            m2.id
    </select>

    <!--获取角色和对应的菜单-->
    <select id="getMenusWithRole" resultMap="MenusWithRole">
        select
            m.id,
            m.url,
            m.path,
            m.component,
            m.name,
            m.iconCls,
            m.keepAlive,
            m.requireAuth,
            m.parentId,
            m.enabled,
            r.id as rid,
            r.name as rname,
            r.nameZh as rnameZh
        from
            t_menu m
        join
            t_menu_role mr
        on
            m.id = mr.mid
        join
            t_role r
        on
            r.id = mr.rid
        order by
            m.id
    </select>

    <!--获取所有菜单-->
    <select id="getAllMenus" resultMap="AllMenus">
        select
            t1.id,
            t1.name,
            t2.id as id2,
            t2.name as name2,
            t3.id as id3,
            t3.name as name3
        from
            t_menu t1
        join
            t_menu t2
        on
            t1.id = t2.parentId
        join
            t_menu t3
        on
            t2.id = t3.parentId
        where
            t3.enabled = TRUE;
    </select>

</mapper>
